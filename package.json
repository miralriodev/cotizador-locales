{
  "name": "cotizador-esscala-monorepo",
  "version": "1.0.0",
  "description": "Monorepo para Cotizador Esscala",
  "scripts": {
    "setup": "npm install && npm install --prefix frontend && npm install --prefix server",
    "start": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix server\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
